services:
  frontend:
    image: node:20
    container_name: fintech-frontend
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "5173:5173"
    command: npm run dev
    networks:
      - fintech-network

networks:
  fintech-network:
    driver: bridge